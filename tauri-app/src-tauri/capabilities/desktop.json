{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Fullscreen control. Desktop only: on mobile the one window is the screen, and tauri::Window has no set_fullscreen there.",
  "platforms": [
    "windows",
    "macOS",
    "linux"
  ],
  "windows": [
    "main"
  ],
  "permissions": [
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen"
  ]
}
