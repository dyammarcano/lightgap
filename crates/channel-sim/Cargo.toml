[package]
name = "channel-sim"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Canal simulado con pérdida, retardo y corrupción, para probar el núcleo sin hardware"

[dependencies]
optical-protocol = { path = "../optical-protocol" }
rand = "0.9"
rand_chacha = "0.9"
