[package]
name = "channel-sim"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Simulated channel with loss, delay and corruption, to test the core without hardware"

[dependencies]
optical-protocol = { path = "../optical-protocol" }
rand = "0.9"
rand_chacha = "0.9"
