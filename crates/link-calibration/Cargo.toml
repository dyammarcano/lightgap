[package]
name = "link-calibration"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Profile search, goodput scoring and adaptive control, agnostic to the medium"

[dependencies]
optical-protocol = { path = "../optical-protocol" }
