[package]
name = "modem"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Composes protocol, codec and calibration into a working transfer engine"

[dependencies]
optical-protocol = { path = "../optical-protocol" }
optical-codec = { path = "../optical-codec" }
link-calibration = { path = "../link-calibration" }
blake3 = "1"
thiserror.workspace = true

[dev-dependencies]
channel-sim = { path = "../channel-sim" }
