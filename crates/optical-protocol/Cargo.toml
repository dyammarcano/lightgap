[package]
name = "optical-protocol"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Núcleo sans-io del módem multimodal: formato de wire, sesión y fiabilidad"

[dependencies]
crc32fast = "1"
thiserror.workspace = true

[dev-dependencies]
proptest = "1"
